# Byte Bandits MCP Server Configuration
# Copy this file to .env and fill in your values

# Authentication token for MCP server (keep this secret!)
AUTH_TOKEN=your_secret_token_here

# Your phone number in format {country_code}{number}
# Example: 919876543210 for +91-9876543210
MY_NUMBER=919876543210

# Optional: Server configuration
# PORT=8086
# HOST=0.0.0.0

# Optional: Web request timeout (seconds)
# WEB_TIMEOUT=30

# Redis configuration for Emotion Therapy sessions
# Set REDIS_URL to point to your Redis instance
# Example for local docker-compose: redis://localhost:6379
REDIS_URL=redis://localhost:6379
# Session TTL in seconds (default 3 days)
THERAPY_SESSION_TTL=259200

# Therapy feature flags
# Auto-run diagnostic questions after /feel
THERAPY_AUTO_WHY=0
# Use LangChain-backed LLM provider (requires OPENAI_API_KEY and installing extras [langchain])
THERAPY_USE_LANGCHAIN=0

# OpenAI API key (either this or OPEN_API_KEY will be used)
# OPENAI_API_KEY=sk-your-key
# Compatibility alias; server maps this to OPENAI_API_KEY if set
# OPEN_API_KEY=sk-your-key

# JWT Token Generator (HTML)
# Secret used to sign JWTs for phone numbers (change in production!)
JWT_SECRET=change-me-dev-secret
# Token validity in days (default 14 = 2 weeks)
JWT_EXPIRATION_DAYS=14
# Mounting behavior: when supported, the token app mounts into main app at this path
# TOKEN_APP_PATH=/
# If mounting is unavailable, a separate server starts at this port. 0 => PORT+1
# TOKEN_APP_PORT=0
